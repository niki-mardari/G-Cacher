-- Optional database notes for SatCache.
-- The FastAPI backend creates these tables automatically on startup.
-- This file is mounted into the PostgreSQL container for future migrations/seeding.

-- You can open the database from the host with:
-- docker exec -it satcache_db psql -U satcache_user -d satcache_db
