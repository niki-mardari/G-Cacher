-- SatCacher database notes.
-- The FastAPI backend creates the tables when it starts.
-- This file is mounted into PostgreSQL for future manual migrations if needed.

-- Useful command:
-- docker exec -it satcache_db psql -U satcache_user -d satcache_db
